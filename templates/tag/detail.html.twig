{% extends 'base.html.twig' %}

{% block title %}{{ tag.name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="text-center mt-4">
            <h1>{{ tag.name }}</h1>
        </div>
    </div>
{% endblock %}