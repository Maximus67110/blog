{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <div class="container mt-2">
        <a class="btn btn-outline-secondary" href="{{ path('app_tag_list') }}">Back</a>
        <div class="text-center mt-4">
            <h1>Edit Tag</h1>
        </div>
        {{ form(form) }}
    </div>
{% endblock %}
